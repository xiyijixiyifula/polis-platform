# Bug 修复时间线 2026

> 记录所有线上 Bug 修复，按时间倒序。每次修复后，维护者应在此追加一条记录。

---

## 2026-05-29

| 版本 | 类型 | 描述 | Pattern | 回归风险 | 修复人 |
| v1.0.43 | 复发 | **模块Route回退回归修复** — 概览区 `MODULE_CONFIG[p.module_type]?.route \|\| 'posts'` 导致自定义模块（不在MODULE_CONFIG中）帖子泄露到"交流"Tab。修复: `'posts'` → `p.module_type` | [module-breadcrumb-hardcoded](../patterns/module-breadcrumb-hardcoded.md) | ⚠️ v1.0.41 引入的回归 — 概览区 route fallback 不能硬编码 'posts' | @xiyijixiyifula |
| v1.0.42 | Bug | **Profile页自定义模块名显示module_key** — creation_to_public() 新增 LEFT JOIN space_modules 返回 module_name，前端 adaptCreationItem() 读取 module_name 作为 moduleLabel | [module-breadcrumb-hardcoded](../patterns/module-breadcrumb-hardcoded.md) | ⚠️ SubmissionInfo 新增字段影响所有 downstream 消费者 | @xiyijixiyifula |
| v1.0.41 | Bug | **模块架构去交流中心主义彻底改造** — 25+处'forum'/'交流'硬编码回退根因修复。核心库(module-config.ts)4函数/常量修正 + 4组件/页面散落硬编码替换 + 后端feed API新增module_name字段。修复范围: 8文件20+点位。根因: MODULE_CONFIG是静态封闭字典但系统需处理动态开放模块集合。 | [module-breadcrumb-hardcoded](../patterns/module-breadcrumb-hardcoded.md) | ⚠️ 新增模块相关功能时: 1)不硬编码'forum'/'交流'fallback 2)使用getModuleLabel() 3)自定义模块API返回module_name | @xiyijixiyifula |
| v1.0.40 | Bug | 社区概览页和Feed页帖子面包屑显示模块名为'交流'而非实际模块名(如'天气预报') — 表面修复（PostCard+SpacePageClient） | [module-breadcrumb-hardcoded](../patterns/module-breadcrumb-hardcoded.md) | 所有使用getModuleLabel()和PostCard的页面，自定义模块键无法在MODULE_CONFIG中找到时会fallback到'交流' | @xiyijixiyifula |
| v1.0.39 | Bug | 自定义模块帖子DB中module_type正确但API返回forum/text，前端tab不显示 | [enum-serialization-data-loss](../patterns/enum-serialization-data-loss.md) | 所有serde_json::from_str().unwrap_or_default()枚举反序列化丢失未知值，影响所有自定义模块/内容类型 | @xiyijixiyifula |
|------|------|------|---------|----------|--------|
| v1.0.33 | Feature | **创作体验简化** — 模块类型从 17 种简化为 2 种（文章/视频），移除 unlisted 可见性，移除密码分享，移除 AI 对话(Thread)/问答(QA)编辑器。个人主页作品Tab改为固定 概览/视频/文章 三个子tab。 | — | ⚠️ 现有 unlisted 帖子编辑可见性选项保留兼容 | @xiyijixiyifula |
| v1.0.33 | Bug | **mtFilter 硬编码模块类型过滤** — SpacePageClient 使用硬编码 Set 过滤帖子模块类型，自定义模块帖子被过滤掉无法显示。修复: 移除 mtFilter，直接追加所有帖子。 | — | ⚠️ 未来新增帖子 API 响应字段时检查过滤逻辑 | @xiyijixiyifula |
| v1.0.34 | Bug | **模块Tab点击后内容区域空白** — 动态 Tab 使用 `m.module_key` 作为 id（如 `forum`），但渲染块匹配 `activeTab === 'posts'` 等 route 名。两个键空间不一致导致 Tab 激活但无匹配渲染块。自定义模块 key 完全无任何渲染块匹配。修复: tab id 改为 `MODULE_CONFIG[m.module_key]?.route \|\| m.module_key` + 新增通用 fallback 渲染块。 | [module-tab-key-mismatch](../patterns/module-tab-key-mismatch.md) | ⚠️ 新增模块类型时确保 MODULE_CONFIG 映射 + KNOWN_TABS 集合同步 | @xiyijixiyifula |
| v1.0.35 | Defense | **SpaceSettings allowed_content_types null 安全加固** — Line 186: `m.allowed_content_types.map()` 缺少空值防御，若 API 返回 null 则 React 崩溃白屏。修复: `(m.allowed_content_types ?? []).map()`。虽经多次浏览器测试未能复现用户报告的"改模块权限后页面空白"，但此防御性修复消除了潜在崩溃点。 | [array-map-null](../patterns/array-map-null.md) | ⚠️ 所有 `.map()` 调用应使用 `?.` 或 `?? []` 防空 | @xiyijixiyifula |

## 2026-05-28

| 版本 | 类型 | 描述 | Pattern | 回归风险 | 修复人 |
|------|------|------|---------|----------|--------|
| v1.0.28 | Bug | **管理页"管理"按钮无响应（首次尝试，未解决）** — 推测为 React 批处理竞态导致 isOwner 校验失败。修复：将 isOwner 校验从独立 useMemo/useEffect 移入 API 回调。部署后问题仍存在。 | — | ⚠️ 症状推测错误，React 批处理非根因 | @xiyijixiyifula |
| v1.0.29 | Bug | **管理页"管理"按钮无响应（真正修复）** — JWT token 使用 base64url 编码（含 `-` 和 `_`），但 JavaScript 的 `atob()` 仅支持标准 base64，导致解码抛异常 → catch 重定向回社区页。修复: `atob()` 前将 `-` 替换为 `+`，`_` 替换为 `/`。同时修复 PostPageClient.tsx 同类型问题。 | [atob-base64url](../patterns/atob-base64url.md) | ⚠️ 所有使用 atob() 解码 JWT 的前端代码 / 新增 JWT payload 解析时需检查 URL-safe 转换 | @xiyijixiyifula |
| v1.0.30 | Feature | **自定义模块系统 (前半)** — space_modules 表 + 5 个 repo 方法 + handler + routes + SpaceModulesManager 组件重写 + 动态 Tab + 创作页模块联动 | — | ⚠️ 路由覆盖完整性 / 前端动态模块 key 匹配 | @xiyijixiyifula |
| v1.0.31 | Bug | **Gateway 路由误判** — `proxy_space_router` 中 `is_content` 条件包含 `remaining.contains("/modules")`，导致 `/api/spaces/{ns}/modules` 被路由到 content 服务而非 space 服务，返回 404。修复: 移除 modules 条件。 | [gateway-route-missing](../patterns/gateway-route-missing.md) | ⚠️ 新增 space 端点时检查 Gateway is_content/is_video 条件 | @xiyijixiyifula |
| v1.0.32 | Bug | **模块端点 "Space not found"** — `handle_auth_path` 的 `actions` 数组遗漏 `/modules` 后缀，导致 namespace 提取失败（ns 包含 `/modules`）。修复: actions 数组追加 "/modules"。 | [actions-array-missing](../patterns/actions-array-missing.md) | ⚠️ 所有 handle_auth_path 处理的新端点必须更新 actions 数组 | @xiyijixiyifula |
| v1.0.32 | Bug | **DELETE /modules 返回 404** — 路由配置 `.delete(delete_space)` 将所有 DELETE 请求路由到 `delete_space` 函数，而非 `handle_auth_path`。修复: 改为 `.delete(handle_auth_path)`，space 归档逻辑移入 handle_auth_path。 | — | ⚠️ 新增带 DELETE 的端点时确认路由 handler 匹配 | @xiyijixiyifula |
| v1.0.32 | Bug | **编译目标错误（无效部署）** — `cargo build --release` 默认编译 macOS 二进制，Linux 服务器二进制从未更新。修复: 改用 `--target x86_64-unknown-linux-gnu` 重新编译。 | [wrong-build-target](../patterns/wrong-build-target.md) | ⚠️ 每次编译时显式指定 Linux target / 打包前验证 ELF 格式 | @xiyijixiyifula |
| v1.0.32 | Deploy | **前端旧代码残留** — v1.0.30 打包时 SpaceSettings.tsx 新代码未被包含（时间戳问题），服务器仍是旧版。修复: 重新 `npx next build` + 打包部署。 | — | ⚠️ 每次部署后验证前端版本（检查特定 DOM 元素是否出现） | @xiyijixiyifula |

## 2026-05-27

| 版本 | 类型 | 描述 | Pattern | 回归风险 | 修复人 |
|------|------|------|---------|----------|--------|
| v1.0.17 | Bug | 创建社区标题 slug 化修复 — title: slug → title.trim() + deriveSlug 保留下划线 | — | — | @xiyijixiyifula |
| v1.0.17 | Bug | 前端添加删除社区按钮 — Trash2 图标 + Fragment 包裹 | — | — | @xiyijixiyifula |
| v1.0.16 | Feature+Bug | 社区功能增强 (v1.0.15) 生产部署 — 关注+图标上传+私有空间权限 | — | ⚠️ 图标/封面 URL 清除逻辑 (CASE WHEN 空值) / 关注计数本地更新 | @xiyijixiyifula |
| v1.0.16 | Security | 私有空间发帖权限门控 + 文件上传权限门控 — 非成员 403 | — | ⚠️ 防止非成员绕过权限向私有社区发帖或上传文件 | @xiyijixiyifula |
| v1.0.18 | Feature | 内容审核系统 — 平台级封禁 (banned) + 时限隐藏 (hidden_until) + 批量操作 + 登录门控 + Auto-Restore | — | ⚠️ Visibility::Hidden 枚举序列化 / hidden_until 时区比较 (TIMESTAMPTZ vs Utc::now) | @xiyijixiyifula |
| v1.0.18 | Bug | Visibility 枚举缺失 Hidden 变体 — serde_json 反序列化失败 fallback 到 Public 默认值，导致隐藏帖子 API 返回 public | — | ⚠️ 新增 DB visibility 值时同步刷新 Visibility enum | @xiyijixiyifula |
| v1.0.18 | Bug | PostPublic 使用 post.visibility (原始DB值) 而非 effective_visibility (考虑 auto-restore)，导致首次 GET 返回过期 visibility | — | ⚠️ 后端数据二次处理后需用处理后的值构造响应 | @xiyijixiyifula |
| v1.0.19 | Feature | 管理后台增强 + Agent 审查系统 — 3个新页面 (审查队列/审查规则/操作日志) + 3个页面增强 + Agent 审查 API + 种子规则 | — | ⚠️ review_rules CRUD / Agent API 认证 / 前端表单字段完整性 | @xiyijixiyifula |
| v1.0.21 | Bug | 系统设置页验证码修改失败时显示 "Authentication required" 而非 "当前验证码不正确" — update_admin_code_handler 错误使用了 AppError::Unauthorized (401) 而非 AppError::Validation (400) | — | ⚠️ API 错误类型映射：业务校验错误用 Validation(400)，仅认证失败用 Unauthorized(401) | @xiyijixiyifula |
| v1.0.20 | Bug | 管理后台登录页面缺少密码输入框 — 表单 state 有 password 字段但 UI 无 input，导致登录无法通过 Argon2 验证 | [missing-form-field](../patterns/missing-form-field.md) | ⚠️ 任何表单重构后逐字段对比 UI input 与 form state | @xiyijixiyifula |
| v1.0.22 | Bug | 未认证用户无封禁按钮 — 管理后台用户页仅对已认证用户显示封禁按钮，管理员无法封禁未认证用户 | — | ⚠️ 权限按钮条件判断不以认证状态为前置条件 | @xiyijixiyifula |
| v1.0.22 | Bug | 封禁原因硬编码为"违规操作" — 管理员无法自定义封禁原因 | — | ⚠️ 所有管理操作表单预留自定义输入字段 | @xiyijixiyifula |
| v1.0.22 | Bug | Gateway 缺少 `/api/user/` 路由 — 新增的 ban-status 和 appeal API 端点经网关返回 404 | [gateway-route-missing](../patterns/gateway-route-missing.md) | ⚠️ 新增 API 路由时同步更新 Gateway 路由表 | @xiyijixiyifula |
| v1.0.22 | Feature | 用户申诉系统 — ban-status API + appeal API + 申诉前端页 + 管理后台申诉处理 + 登录页申诉入口 | — | ⚠️ 申诉复用 reports 表 (target_type='appeal')，无新 migration | @xiyijixiyifula |
| v1.0.23 | Bug | 登录页申诉链接仅在错误信息含"封禁"/"冻结"时显示 — 自定义封禁原因不包含这些词导致申诉入口不显示。修复：增加 `Forbidden:` 前缀检测 | — | ⚠️ 错误消息匹配用结构性前缀而非语义关键词 | @xiyijixiyifula |
| v1.0.24 | Feature | **平台设置系统** — `platform_settings` 表 + Admin API (GET/PUT `/api/admin/settings/platform`) + 前端设置页上传大小配置 UI。解决上传大小硬编码、附件无大小限制问题 | — | ⚠️ 新增 API 路由时同步更新 Gateway 路由表。修改平台设置后需重启对应服务 | @xiyijixiyifula |
| v1.0.24 | Security | **上传安全加固** — 内容服务 `upload_file()` / `upload_file_generic()` 新增 DB 动态大小检查 + 路由层 DefaultBodyLimit + Gateway body limit 可配置 (`GATEWAY_UPLOAD_LIMIT_MB`/`GATEWAY_VIDEO_LIMIT_MB` env vars) | — | ⚠️ Gateway body limit 为启动时静态值，修改平台设置后需重启 gateway | @xiyijixiyifula |
| v1.0.24 | Cleanup | **网站初始化清理** — 删除 66 个 E2E 测试用户 + 89 个测试空间 + 登录页移除测试账号提示 | — | ⚠️ 清理涉及 67 个 FK 约束的表，需按依赖顺序删除 | @xiyijixiyifula |
| — | Issue | **视频无法播放 — HLS 文件丢失**。服务器 `data/hls/` 和 `data/videos/` 目录为空，所有已上传视频的原始文件和 HLS 流文件均已丢失。API 返回正常（数据库记录完整），但磁盘文件不存在。非代码 Bug，是数据持久化问题。 | — | ⚠️ 部署/迁移时确保 `data/` 目录完整备份。视频上传功能正常（新上传可用），但历史视频无法恢复。 | @xiyijixiyifula |
| v1.0.25 | Bug | **polisctl/adminctl 硬编码密码修复** — polisctl admin login 硬编码 `admin123`，adminctl.sh 硬编码空密码。改为参数传入。 | — | ⚠️ 管理工具密码不应硬编码 | @xiyijixiyifula |
| v1.0.25 | Bug | **管理码不一致** — 服务器 `admin_code.txt` 存储 `polis2026`，但 systemd 环境变量为 `mzGW2026!PolisHub`。代码优先读文件，导致凭据与文档不一致。 | — | ⚠️ 管理码修改后文件与环境变量不同步 | @xiyijixiyifula |
| v1.0.25 | Test | **全功能深度测试** — 12 个管理后台页面 + 所有公开页面 + API 端点 + 申诉流程，全部正常。无新增 Bug。 | — | — | @xiyijixiyifula |
| v1.0.26 | Feature | **社区 Star 收藏系统** — space_stars 表 + 7 个 repo 方法 + 4 个 API 端点 + 前端 Star 按钮 + Saved 页收藏社区展示。Star 数据参与 Trending 排序加权（star_count × 0.4）。 | — | ⚠️ Star/Unstar 前端幂等（唯一约束防重复）/ star_count 显示需部署到 stats 行 | @xiyijixiyifula |
| v1.0.26 | Feature | **社区管理页** — `/space/manage/[...namespace]` 独立管理页（5个Tab：基本信息/模块设置/成员管理/加入审批/数据概览）。仅社区 owner 可访问，非 owner 重定向回社区页。 | — | ⚠️ 路由冲突风险：manage 路由从 `[...namespace]/manage` 移到 `manage/[...namespace]` 避免 Next.js 路由排序冲突 | @xiyijixiyifula |
| v1.0.27 | Bug | **部署：服务二进制路径不匹配** — 部署脚本将二进制写入 `/usr/local/bin/`，但 systemd 服务实际使用 `/root/polis/target/release/`。导致新二进制未生效。需先停止服务再覆盖文件。 | — | ⚠️ 部署前确认 systemd ExecStart 路径与部署目标路径一致。覆盖运行中文件需先 systemctl stop | @xiyijixiyifula |
| v1.0.27 | Fix | **star_count 前端显示** — stats 行新增"收藏"计数（Star 图标 + formatCount(space.star_count)），补充部署遗漏的显示部分。 | — | — | @xiyijixiyifula |
| v1.0.27 | Config | **上传大小限制调整** — 附件从 50MB 降至 10MB，视频从 500MB 降至 200MB（服务器 1.6GB 内存）。通过管理后台 /admin/settings 即可修改。 | — | ⚠️ 修改平台设置后需重启 polis-gateway 和 polis-video 使 body limit 生效 | @xiyijixiyifula |

## 2026-05-26

| 版本 | 类型 | 描述 | Pattern | 回归风险 | 修复人 |
|------|------|------|---------|----------|--------|
| v1.0.14 | Bug | 社区功能 5 项修复 — title 空值+超长校验 + 软删除 DELETE + post_count 双路径同步 + members keyMap | [post-count-sync](../patterns/post-count-sync.md) | ⚠️ 新增 posts 创建路径时需同步 / 删除幂等性 | @xiyijixiyifula |
| v1.0.13 | Security | 安全审计全面修复 — JWT exp 显式校验 + Argon2 密码哈希 + SQL 参数化 + zip-slip + XSS 过滤 + 安全头加固 | — | ⚠️ JWT 跨服务校验一致性 / Argon2 密码迁移兼容 | @xiyijixiyifula |
| v1.0.12 | Bug | cherry-markdown 编辑器报错 — 依赖自动升级到 0.11.2 不兼容 | [dependency-auto-upgrade](../patterns/dependency-auto-upgrade.md) | ⚠️ 其他 `^` 依赖 | @xiyijixiyifula |
| v1.0.11 | 复发 | 个人主页作品Tab为空 — username 双重 URL 编码 | [url-double-encoding](../patterns/url-double-encoding.md) | 同文件其他 API 调用 | @xiyijixiyifula |
| v1.0.9 | Bug | 首页 Feed 关注 Tab key 不匹配 (`'follow'` → `'following'`) | — | — | @xiyijixiyifula |
| v1.0.9 | Bug | Feed 热榜排序被 `created_at DESC` 覆盖 | — | — | @xiyijixiyifula |

## 2026-05-25

| 版本 | 类型 | 描述 | Pattern | 修复人 |
|------|------|------|---------|--------|
| v1.0.8 | Bug | 前端安全加固 — 14处 .map() 防空 + console.error 清理 | [array-map-null](../patterns/array-map-null.md) | @xiyijixiyifula |
| v1.0.8 | Bug | JWT_SECRET 缺少强制验证 | — | @xiyijixiyifula |
| v0.3.86 | Bug | CI 构建修复 — package-lock + hls.js 依赖 | — | @xiyijixiyifula |
| v0.3.85 | Bug | SEO 页面标题双重 "| Polis" 后缀 | — | @xiyijixiyifula |

## 2026-05-23

| 版本 | 类型 | 描述 | Pattern | 修复人 |
|------|------|------|---------|--------|
| v0.3.72 | 复发 | 内容管理页 `m.map is not a function` | [array-map-null](../patterns/array-map-null.md) | @xiyijixiyifula |
| v0.3.64 | Bug | 管理后台 `reported_content` SQL — 子查询错误使用 `posts` 表 | — | @xiyijixiyifula |
| v0.3.63 | Bug | Agent 社区路由冲突 — `/api/spaces/{id}/agents` → `/api/communities/{id}/agents` | — | @xiyijixiyifula |

## 2026-05-22

| 版本 | 类型 | 描述 | Pattern | 修复人 |
|------|------|------|---------|--------|
| v0.3.58 | Bug | ESC 退出全屏编辑器 + 创作中心一键投稿 | — | @xiyijixiyifula |
| v0.3.56 | Bug | 首页 Feed 排序算法 + 探索/推荐社区热度排序 | — | @xiyijixiyifula |
| v0.3.54 | Bug | 系统全面修复 — DDL + unwrap + SEO + Image + hls.js SSR | — | @xiyijixiyifula |

## 2026-05-21~22

| 版本 | 类型 | 描述 | Pattern | 修复人 |
|------|------|------|---------|--------|
| v0.3.50 | Bug | 服务器 OOM 宕机 — `npm run build` + `cargo build` 吃满 1.6GB | — | @xiyijixiyifula |

## 更早

| 版本 | 类型 | 描述 | Pattern | 修复人 |
|------|------|------|---------|--------|
| v0.3.95 | 复发 | macOS xattr 部署污染（第2次） | [xattr-contamination](../patterns/xattr-contamination.md) | @xiyijixiyifula |
| v0.3.91 | 首次 | macOS xattr 部署污染（第1次） | [xattr-contamination](../patterns/xattr-contamination.md) | @xiyijixiyifula |
| v0.3.81 | Bug | 种子数据依赖根除 — E2E 动态注册 | — | @xiyijixiyifula |
| v0.2.84 | Bug | 微服务缺少 `/health` 端点 | — | @xiyijixiyifula |
| v0.2.68 | Bug | 评论匿名 — create_comment 返回无 author 字段 | — | @xiyijixiyifula |
| v0.2.58 | 首次 | 内容服务中文路由 404（后端） | [url-double-encoding](../patterns/url-double-encoding.md) | @xiyijixiyifula |
| v0.2.57 | 首次 | 模块设置 localStorage key 迁移 | — | @xiyijixiyifula |
| v0.2.54 | 首次 | 中文 slug 社区 404（后端） | [url-double-encoding](../patterns/url-double-encoding.md) | @xiyijixiyifula |
| v0.2.7 | Bug | 网关查询参数丢失 — `proxy_to_*` 未用 `path_and_query()` | — | @xiyijixiyifula |
