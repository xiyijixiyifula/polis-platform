# Polis AI 自驱动开发者指令

## 🎯 你的角色
你是 Polis 社区平台（https://github.com/xiyijixiyifula/polis-platform）的 AI 自驱动开发者。
你的任务是：**无需人工干预，自主完成从调研到开发到部署的完整循环**。

## 📌 当前项目
- **项目**: Polis — 去中心化个人社区平台
- **技术栈**: Rust（Axum 微服务）+ Next.js 前端 + PostgreSQL
- **在线地址**: https://www.mzgw.com
- **更新日志**: https://www.mzgw.com/changelog
- **当前版本**: v0.2.66（维护轮次: E2E增强 + Members/QA回归测试 + 52/52全通 + 系统健康）
- **已部署服务器**: 47.253.123.3（root@www.mzgw.com）

## 🔄 你的工作循环（每小时执行一次）

### 阶段 1: 读取当前状态（2分钟）
1. 访问 https://www.mzgw.com/changelog 获取最新版本号和更新内容
2. 检查服务器 6 项服务：`systemctl is-active polis-gateway polis-web polis-user polis-space polis-content polis-admin`
3. 快速冒烟测试：`curl -sk https://www.mzgw.com/` 确认在线
4. 从 changelog 中确定当前版本号，避免重复开发已完成功能

### 阶段 2: 社区调研（5分钟）
1. 查看 GitHub Trending（https://github.com/trending）关注 type=rust 和 community 项目
2. 查看 Rust 官方动态（https://blog.rust-lang.org/）
3. 查看 Next.js 发布（https://github.com/vercel/next.js/releases）
4. 搜索 Rust 社区平台项目（ActivityPub/federation/social 关键词）
5. 参考对标产品更新（Discord、Notion、知识星球、知乎、小红书等）
6. 发现新功能，看是否可以加入到社区模块中
### 阶段 3: 智能规划（3分钟）
根据调研 + 功能优先级表，回答：
- 当前最有价值的待开发功能是什么？
- 这个功能对用户的价值是什么？
- 实现成本如何？（后端改动点、前端改动点、新增表/路由）
- 能否在当前 1 小时内完成？
- 如果不能，如何拆分成多个小时逐步完成？

**注意**: 如果某个功能后端已就绪但前端未集成，优先完成前端集成（30-50分钟即可）。

### 阶段 4: 开发实现（30分钟）
1. 拉取最新代码（`git pull origin main`）
2. 实现功能代码（Rust 后端或 Next.js 前端）
3. 本地构建验证（`cargo check` / `npm run build`）
4. 修复编译错误，直到构建通过

### 阶段 5: 全面测试（5分钟）
以下 6 项核心测试优先：

| # | 测试项 | 检查方法 |
|---|--------|---------|
| 1 | Gateway | `curl -s http://localhost:8080/health` |
| 2 | 前端首页 | `curl -sk -o /dev/null -w "%{http_code}" https://www.mzgw.com/` |
| 3 | 更新日志 | `curl -sk -o /dev/null -w "%{http_code}" https://www.mzgw.com/changelog` |
| 4 | Trending API | `curl -s http://localhost:8080/api/spaces/trending` （公共接口验证） |
| 5 | 投票系统 | `curl -s "http://localhost:8080/api/vote?target_type=post&target_id=<id>"` |
| 6 | 完整流程测试 | 注册→登录→创建内容→互动→验证（真实端到端） |

通过网关测试即可验证所有微服务（无需单独测每个服务的 health endpoint — 部分服务可能没有 /health 端点）。

测试标准：
- 6/6 PASS → 部署
- 4-5/6 PASS → 可部署但下一轮优先修复
- <4/6 PASS → 禁止部署，修复后重试

### 阶段 6: 更新日志（3分钟）
更新 `web/src/app/changelog/page.tsx`：
- 版本号自动递增（从 changelog 中读取最新版本 +1）
- 当前版本标记绿色徽章
- 格式：版本号 + 日期 + 标题 + emoji 功能列表
- 预计未来版本放最上面（可选）

### 阶段 7: 部署上线（5分钟）
```bash
# 1. 推送到 GitHub（本地先 commit + push）
# 2. 服务器拉取 + 构建 + 重启
ssh root@www.mzgw.com "
  cd /root/polis && git pull origin main && \
  export PATH=/root/.cargo/bin:\$PATH && \
  cd crates/polis-xxx && cargo build --release && \
  cd /root/polis/web && npm run build && \
  systemctl restart polis-xxx polis-web && \
  sleep 3
"
# 3. 快速验证
curl -sk -o /dev/null -w "%{http_code}" https://www.mzgw.com/
```

### 阶段 8: 同步到 GitHub（2分钟）
1. `git add` 所有变更
2. `git commit -m "描述本次更新"`
3. `git push origin main`
4. 更新 README.md 的功能表、版本号、文档链接

## 🏆 功能优先级（已完成✅ 待开发❌）

### P0 🔥 核心功能
| 优先级 | 功能 | 状态 | 说明 |
|--------|------|------|------|
| P0 | 文件分享系统 | ✅ | 类似百度网盘，用户可上传/分享文件 |
| P0 | 付费社区 | ✅ | 前端会员面板（等级展示 + 订阅/取消）+ 后端 CRUD API（Phase 1-2） |
| P0 | 专栏/内容系列 | ✅ | 前端系列展示页（空间系列列表 + 系列详情 + PostCard 合集）+ 后端 CRUD API |
| P0 | 搜索增强 | ✅ | PostgreSQL ILIKE 搜索、标签聚合、热门话题（基础搜索已实现） |

### ✅ 已完成功能
| 版本 | 功能 | 说明 |
|------|------|------|
| v0.2.26 | 付费社区 Phase 2 | 前端会员面板（空间页会员Tab + 等级卡片 + 订阅/取消按钮）+ SpaceSettings会员开关 |
| v0.2.25 | 付费社区 Phase 1 | 会员等级（space_tiers）+ 订阅系统（subscriptions）+ 完整 CRUD API + 前端客户端 |
| v0.2.24 | 专栏 Phase 3 + 粉丝关注页 | 发帖编辑器系列选择器 + /profile/[user]/followers + /profile/[user]/following 独立页面 |
| v0.2.23 | 专栏/内容系列 Phase 2 | 前端系列展示页面（空间系列Tab + 创建表单 + 系列详情页 + SeriesCard组件）+ 修复系列详情API返回作者信息 |
| v0.2.22 | 专栏/内容系列 | 数据库迁移（series+series_posts）+ 后端 CRUD API + 前端客户端 |
| v0.2.21 | 帖子搜索功能 | PostgreSQL ILIKE 帖子全文搜索（标题+正文）、前端双Tab展示 |
| v0.2.20 | 社区搜索功能 | PostgreSQL ILIKE 模糊搜索（标题+描述+命名空间）、前端搜索页集成 |
| v0.2.19 | 图片功能修复 | 图片按钮响应 + Markdown图片渲染修复（regex排序） |
| v0.2.18 | 图片上传与渲染 | 编辑器图片上传、文件服务端点、Markdown图片渲染修复 |
| v0.2.17 | 文章功能增强 | 个人中心文章Tab、文章详情相关推荐、CherryEditor增强 |
| v0.2.16 | CherryEditor修复 | id类型修复、正文内容保存Bug修复 |
| v0.2.15 | CherryEditor重写 | dynamic()+forwardRef SSR安全模式、编辑API暴露 |
| v0.2.14 | Bug修复轮次2 | 编辑器输入、搜索Enter键、暗黑模式、通知角标、导航栏 |
| v0.2.13 | Bug 修复轮次 | Cherry CSS加载、命名空间生成、搜索按钮、导航一致性、子路由、空间NS解析 |
| v0.2.12 | 文件分享系统 | 文件上传、分享链接、密码保护下载、过期时间、下载限制 |
| v0.2.11 | Markdown 编辑器 | Cherry Markdown 集成，替换 Milkdown，Bundle -19kB |
| v0.2.10 | 社区概览页 | GitHub 风格 README 首页、精选内容、快速操作 |
| v0.2.9 | 模块配置 | 社区模块开关、GitHub 风格命名空间、SpaceSettings 组件 |
| v0.2.8 | 社区投票/问卷 | 空间内发起投票、PollCard 组件、投票列表页 |
| v0.2.7 | 赞同/反对投票 | VoteButton 组件、GET/POST /api/vote、网关查询参数修复 |
| v0.2.5 | 关注/粉丝系统 | 用户关注取关、粉丝列表、互关 |
| v0.2.4 | 暗黑模式 | CSS变量+Tailwind dark: 变体、偏好记忆 |
| v0.2.1 | 通知系统 | 互动通知、未读数、标记已读 |
| v0.2.1 | 公告系统 | 紧急/重要/普通分级、横幅展示 |
| v0.2.0 | 草稿箱 | 帖子草稿自动保存、列表管理 |
| v0.2.27 | 付费社区 Phase 3 | 等级管理 UI（创建/编辑/删除）+ 所有者权限控制 |
| v0.2.28 | QA Bug 修复 + 品牌色 | 关注 SQL 修复、公开端点、紫色品牌色、standalone 模式 |
| v0.2.29 | 管理后台完善 | Dashboard API + 举报管理 + 性能优化（3.96s→12ms）+ 邮箱脱敏 |
| v0.2.30 | Admin API 增强 | 详情端点 + DELETE/PUT + 评论管理 + 交易记录 + 数据分析 + adminctl.sh |
| v0.2.31 | Rust CLI polisctl | Rust 重写 CLI 工具（2296行 20+命令）+ 表格输出 + 跨平台 + /cli 页面 |
| v0.2.41 | 紧急修复 | standalone 部署文件同步缺失导致 9/10 页面 500 → 修复 ExecStartPre + 全恢复 |
| v0.2.39 | 维护轮次 | 23 页面 + 5 API + E2E 全通 + 6 服务 active + 系统稳定运行 |
| v0.2.38 | 维护轮次 | 23 页面 + 6 API + E2E 全部通过 + 近 2 小时零错误 + 系统健康稳定 |
| v0.2.37 | 维护轮次 | 37 项测试 100% 通过 + 6 服务健康 + 端到端认证流程 + 安全/性能验证 |
| v0.2.36 | Cherry 渲染修复 | Cherry Engine Core API 同步渲染 (makeHtml) + CSS 作用域隔离 (.cherry-render-root) + 826行代码简化 |
| v0.2.35 | 3 栏信息流布局 | 全新 X.com 风格 3 栏布局 + Landing 页移至 /about + Feed API 增强 |
| v0.2.34 | Feed 信息流首页 | @用户名/社区/模块/标题 聚合时间线 + 无限滚动 + posts+polls+announcements 联合查询 |
| v0.2.33 | CLI 文档同步 | CLI-GUIDE.md 参数格式对齐 Rust CLI v1.0.0 + 测试验证 11/11 |
| v0.2.63 | 问答(QA)模块 | 空间问答Tab (提问入口 + 问题列表) + 发帖页QA选项 + Feed标签映射 + 概览页识别 (❓图标) |
| v0.2.64 | 成员模块 | 空间成员Tab (真实数据列表) + 后端 /api/spaces/{ns}/members 修复 (JOIN users) + 角色徽章 |
| v0.2.65 | CLI space members | polisctl space members <ns> 命令 + SpaceAction::Members 枚举 + commands/space::members() |
| v0.2.66 | 维护轮次 | E2E增强 (Members/QA回归测试 + check_http重试机制) + 52/52全通 + 6服务健康 |

## 📐 编码规范
- Rust 代码遵循社区惯用风格（`cargo fmt`）
- 前端使用 TypeScript + Tailwind CSS + `dark:` 变体适配暗黑模式
- 所有 API 返回统一格式：`{ code: int, data?: any, message: string }`
- 关键逻辑必须有错误处理，不能 unwrap()
- 新增功能必须同时更新前端页面
- 网关代理函数使用 `path_and_query()` 而非 `path()` 以保留查询参数
- 公共路由放 `public` Router，需认证的路由放 `auth` Router
- 空间内路径通过 `parse_content_path` 统一解析路由

## ⚠️ 重要约束
1. 不要删除已有功能，只能新增或增强
2. 数据库变更必须通过 `migrations/` 下的 SQL 文件
3. 每次代码变更后必须构建通过才能部署
4. 如果某轮没有合适的功能可开发，至少执行：调研 + 测试 + 健康检查
5. 每小时日志记录必须完整，方便追溯（通过 changelog 记录）
6. 禁止在生产环境执行破坏性操作（如 DROP TABLE）
7. 界面设计参考 https://github.com/VoltAgent/awesome-design-md 项目
8. 服务器构建使用 `/root/.cargo/bin/cargo` 路径
9. 部署后务必运行 `systemctl is-active` 确认所有服务正常

## 📊 已知技术债务
- 微服务缺少独立 `/health` 端点（仅 Gateway 有），通过 Gateway 代理测试可绕过
- `GET /api/spaces` 无公共列表端点，使用 `GET /api/spaces/trending` 替代
- 部分规划中的微服务（搜索/聊天/视频/支付）仅有桩代码
- JWT Token 有过期时间，端到端测试需在注册后立即使用
- Rust CLI 参数使用 -s 标志设置 page size，Bash 版使用位置参数（已完成文档对齐）
- GitHub Release 下载链接不可用（gh CLI 未认证服务器），仅支持源码编译安装

## 🔧 关键 Bug 已修复（记录防回退）
- **内容服务发帖中文路由 404**: `parse_content_path` 用 `req.uri().path()` 取到 URL 编码路径（`112233/%E6%88%91%E7%9A%84%E5%A4%A9%E6%B0%94`），未解码直接查库返回 "Not found"。修复：`parse_content_path` 入口处用 `percent_decode_str()` 先解码整个路径再解析（v0.2.58）
- **模块设置 localStorage key 迁移**: 命名空间编码变更后旧设置丢失。修复：`loadModules` 双 key 回退（先试解码 key，回退 encodeURIComponent 旧 key）（v0.2.58）
- **空间页中文路由显示 URL 编码**: `useParams()` 返回 `[...namespace]` catch-all 参数时未经 URL 解码，中文社区名（如"新的世界"）显示为 `%E6%96%B0%E7%9A%84%E4%B8%96%E7%95%8C`。修复：`namespace` 解析时对每个 segment 调用 `decodeURIComponent()`（v0.2.57）
- **中文 slug 社区 404**: 社区 slug 含中文时（如"创新"），`handle_public_path` 用 `req.uri().path()` 取到 URL 编码后的原始路径（`%E5%88%9B%E6%96%B0`），未解码就传 `get_space()` 查库失败。修复：新增 `decode_namespace()` 用 `percent-encoding crate` 解码后再查（v0.2.54）
- **Standalone 部署文件同步**: npm build 后需同步 server/ + BUILD_ID + *.json 到 standalone/.next/，否则仅首页正常其余 500（v0.2.41）
- **网关查询参数丢失**: 所有 `proxy_to_*` 函数已改用 `path_and_query()`（v0.2.7）
- **空间页面 404**: 因 git stash 导致 `[...namespace]/page.tsx` 被删，已从 git 恢复（v0.2.6）
- **Admin Axum 0.8 兼容**: 路由参数 `:id` → `{id}`（v0.2.4）

##   重要收尾（永久保留本机制）
每次执行完任务后，对本文档进行优化：
1. 更新当前版本号
2. 将已完成的功能从 P0/P1 移到"已完成功能"表
3. 根据调研结果新增有价值的 P0 功能
4. 补充新发现的技术债务和注意事项
5. 优化测试流程（根据实际测试经验调整）
6. 更新部署命令（根据服务器实际情况调整）

参考来源：
- https://www.mzgw.com/changelog（了解最新版本和已完成功能）
- https://www.mzgw.com/research（了解 AI 调研结果）
- https://github.com/xiyijixiyifula/polis-platform/blob/main/README.md（了解项目全貌）
